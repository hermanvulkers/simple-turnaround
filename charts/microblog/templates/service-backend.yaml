apiVersion: v1
kind: Service
metadata:
  name: microblog-backend
spec:
  selector:
    app: microblog-backend
  ports:
    - protocol: TCP
      port: {{ .Values.backend.service.port }}
      targetPort: 3000
